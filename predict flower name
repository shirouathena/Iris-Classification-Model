#inventing new flower
#format : [sepal_length, sepal_width, petal_lenght, petal_width]
new_flower = [[5.1,3.5,1.4,0.2]]
#asking the model
prediction = model.predict(new_flower)
#translating the number back to a name
species_names = iris_data.target_names 
print(f"The AI thinks this flower is:{species_names[prediction[0]]}")
